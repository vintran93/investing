<h1>Portfolios</h1>

  <ol>
  <% @portfolios.each do |portfolio| %>
    <li> <%= link_to portfolio.name, portfolio_path(portfolio) %></li>
  <br>
  <% end %>
  </ol>
<%= link_to "Add A New Portfolio", new_portfolio_path %>

